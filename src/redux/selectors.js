export const isLoggedIn = state => state.user.isLoggedIn;
export const login = state => state.user.data;
export const token = state => state.user.token;
export const modeTheme = state => state.theme.mode;
