import { createContext } from 'react';

export const ThemeContext = createContext(null);
// export const SearchFilmContext = createContext(null);

// export const Context = {
//   theme: createContext(null),
//   searchFilm: createContext(null),
// };
